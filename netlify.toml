[build]
  command = "bun run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback — wszystkie ścieżki serwują index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
